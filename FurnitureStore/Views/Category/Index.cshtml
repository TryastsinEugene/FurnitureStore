@model IEnumerable<Category>
@{
    Layout = "_ProductsLayout";
}

@*@if (ViewBag.EditId == null)
{
    <form asp-action="AddCategory" method="post">
        @Html.Partial("CategoryEditor", new Category())
    </form>
}*@
@*<div class="row flex-lg-nowrap">
        <div class="col mb-3">
            <div class="e-panel card">
                <div class="card-body">
                    <div class="e-table">
                        <div class="table-responsive table-lg mt-3">
                            <table class="table table-bordered">
                                    @foreach (Category category in Model)
                                    {

                                        @if (category.Id == ViewBag.EditId)
                                        {
                                                <form asp-action="UpdateCategory" method="post">
                                                    <input type="hidden" name="Id" value="@category.Id" />
                                                    @Html.Partial("CategoryEditor", category)
                                                </form>
                                        }
                                        else
                                        {
                            <tr>
                                <td>@category.Id</td>
                                <td class="align-middle text-center">
                                    <div class="bg-light d-inline-flex justify-content-center align-items-center align-top" style="width: 35px; height: 35px; border-radius: 3px;"> <img src="/images/@category.CategoryPicture" alt="#" /></div>
                                </td>
                                <td class="text-nowrap align-middle">@category.Name</td>
                                <td class="text-center align-middle">
                                    <form asp-action="Delete" method="post">
                                        <div class="btn-group align-top">
                                            <input type="hidden" name="Id" value="@category.Id" />
                                            <a asp-action="Edit" asp-route-key="@category.Id"
                                               class="btn btn-sm btn-outline-secondary badge">
                                                Edit
                                            </a>
                                            <button class="btn btn-sm btn-outline-secondary badge" asp-action="DeleteCategory" type="submit"><i class="fa fa-trash"></i></button>
                                        </div>
                                    </form>
                                </td>
                            </tr>
                                        }
                                        }
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>*@
<div class="container-fluid mt-3">
    <div class="row">
        <div class="col-1 font-weight-bold">Id</div>
        <div class="col font-weight-bold">Name</div>
        <div class="col font-weight-bold">Picture</div>
        <div class="col-3"></div>
    </div>
    @if (ViewBag.EditId == null)
    {
        <form asp-action="AddCategory" method="post">
            @Html.Partial("CategoryEditor", new Category())
        </form>
    }
    @foreach (Category c in Model)
    {
        @if (c.Id == ViewBag.EditId)
        {
            <form asp-action="UpdateCategory" method="post">
                <input type="hidden" name="Id" value="@c.Id" />
                @Html.Partial("CategoryEditor", c)
            </form>
        }
        else
        {
            <div class="row p-2">
                <div class="col-1">@c.Id</div>
                <div class="col">@c.Name</div>
                <div class="col">@c.CategoryPicture</div>
                <div class="col-3">
                    <form asp-action="DeleteCategory" method="post">
                        <input type="hidden" name="Id" value="@c.Id" />
                        <a asp-action="Edit" asp-route-key="@c.Id"
                           class="btn btn-outline-primary">Edit</a>
                        <button type="submit" class="btn btn-outline-danger">
                            Delete
                        </button>
                    </form>
                </div>
            </div>
        }
    }
</div>
